name: MedAI_Flow_DevSecOps CI/CD Pipeline

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

env:
  ACR_NAME: ${{ secrets.ACR_NAME }}
  AKS_CLUSTER: ${{ secrets.AKS_CLUSTER }}
  RESOURCE_GROUP: ${{ secrets.RESOURCE_GROUP }}

jobs:
  lint_security:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install flake8 bandit

      - name: Lint code
        run: flake8 backend/ ml/ || true

      - name: Security scan
        run: bandit -r backend/ || true

  test:
    runs-on: ubuntu-latest
    needs: lint_security
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: |
          pip install pytest
      - name: Run tests
        run: pytest tests/ || echo "No tests yet"

  build_push:
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Build and Push Docker image
        uses: azure/docker-login@v1
        with:
          login-server: ${{ secrets.ACR_NAME }}.azurecr.io
          username: ${{ fromJson(secrets.AZURE_CREDENTIALS).clientId }}
          password: ${{ fromJson(secrets.AZURE_CREDENTIALS).clientSecret }}
      - run: |
          docker build -t ${{ secrets.ACR_NAME }}.azurecr.io/medai_flow_backend:latest -f backend/Dockerfile .
          docker push ${{ secrets.ACR_NAME }}.azurecr.io/medai_flow_backend:latest

  deploy:
    runs-on: ubuntu-latest
    needs: build_push
    steps:
      - name: Azure Login
        uses: azure/login@v2
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Set AKS Context
        uses: azure/aks-set-context@v3
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}
          cluster-name: ${{ secrets.AKS_CLUSTER }}
          resource-group: ${{ secrets.RESOURCE_GROUP }}

      - name: Deploy to AKS
        run: |
          kubectl set image deployment/medai-backend medai-backend=${{ secrets.ACR_NAME }}.azurecr.io/medai_flow_backend:latest || \
          kubectl create deployment medai-backend --image=${{ secrets.ACR_NAME }}.azurecr.io/medai_flow_backend:latest

