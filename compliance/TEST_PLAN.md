# Software Test Plan (STP)

## Test Levels
- Unit: utils, security, routes.
- Integration: inference pipeline with sample tensors (future).
- System: API endpoints end-to-end.
- Performance: latency SLA validation in CI (future).

## Entry/Exit Criteria
- All unit tests green; coverage > 80% (future gate).
